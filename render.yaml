services:
  - type: web
    name: reviewiq-backend
    env: docker
    rootDir: backend
    plan: free # Change if scaling up
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: CORS_ORIGINS
        sync: false
      - key: SECRET_KEY
        sync: false
